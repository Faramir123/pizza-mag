
const initialState ={
    sortBy: 'popular',
    category: 0
}

const filters =(state=0, action) =>{
    switch (action.type) {
        case value:
            
            break;
    
        default:
            return state;
    } 
}